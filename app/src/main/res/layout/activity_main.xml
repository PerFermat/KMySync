<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Titel und Untertitel stellt die Toolbar hier nicht mehr selbst dar, sondern das
                 Kind-Layout darunter. Der Grund ist die Reihenfolge: Eine Toolbar ordnet ihre Kinder
                 grundsätzlich HINTER Titel und Untertitel ein – eine Lupe links vom Kontonamen wäre
                 damit nicht zu haben. Aufbau wie in nav_header.xml, das dasselbe für die
                 Kontenschublade tut. -->
            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/green_primary"
                android:theme="@style/ThemeOverlay.Ausgaben.Toolbar"
                app:contentInsetStartWithNavigation="0dp"
                app:titleMarginStart="0dp">

                <!-- Ein Layout, nicht zwei: Solange die Lupe neben dem ganzen Block stand, brauchte es
                     eine waagerechte Klammer darum. Seit sie in der Titelzeile sitzt, hätte die nur
                     noch ein Kind – Lint meldet sie zu Recht als überflüssig. -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:gravity="center_vertical">

                    <!-- Die Lupe gehört in diese Zeile, nicht neben den ganzen Block: Sie zeigt an,
                         daß am Kontonamen etwas zu suchen ist, und sitzt dadurch von selbst auf
                         seiner Höhe statt mittig zwischen Name und Untertitel. -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <!-- Nur ein Zeichen, kein Knopf – deshalb klein und ohne Tipp-Welle.
                             Getroffen wird der Kontoname daneben, der dasselbe auslöst. Antippbar
                             bleibt sie trotzdem: Steht das Suchfeld offen, gibt es keinen
                             Kontonamen, und sie ist die einzige Stelle, die die Suche wieder
                             wegräumt. -->
                        <ImageView
                            android:id="@+id/bookingSearchIcon"
                            android:layout_width="20dp"
                            android:layout_height="20dp"
                            android:layout_marginEnd="6dp"
                            android:src="@drawable/ic_search"
                            android:contentDescription="@string/bookings_search_hint"
                            app:tint="@color/white" />

                        <TextView
                            android:id="@+id/toolbarTitle"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:paddingVertical="4dp"
                            android:textColor="@color/white"
                            android:textSize="20sp"
                            android:maxLines="1"
                            android:ellipsize="end"
                            android:background="?attr/selectableItemBackground" />

                        <!-- Tritt während der Suche an die Stelle des Kontonamens – in derselben
                             Zeile, damit die Lupe (dann mit X) daneben stehen bleibt. -->
                        <EditText
                            android:id="@+id/bookingSearch"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:background="@null"
                            android:hint="@string/bookings_search_hint"
                            android:textColor="@color/white"
                            android:textColorHint="#B2FFFFFF"
                            android:textSize="20sp"
                            android:inputType="text"
                            android:imeOptions="actionSearch"
                            android:maxLines="1"
                            android:visibility="gone"
                            android:autofillHints="" />
                    </LinearLayout>

                    <!-- Um Lupenbreite plus Abstand eingerückt, damit die Trefferzahl unter dem
                         Namen beginnt und nicht unter dem Symbol. -->
                    <TextView
                        android:id="@+id/toolbarSubtitle"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="26dp"
                        android:textColor="@color/toolbar_subtitle"
                        android:textSize="13sp"
                        android:maxLines="1"
                        android:ellipsize="end"
                        android:visibility="gone" />
                </LinearLayout>
            </com.google.android.material.appbar.MaterialToolbar>

            <LinearLayout
                android:id="@+id/saldoHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:paddingVertical="12dp"
                android:clickable="true"
                android:focusable="true"
                android:background="@color/saldo_bar_bg"
                android:foreground="?attr/selectableItemBackground">

                <TextView
                    android:id="@+id/textSaldoLabel"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:singleLine="true"
                    android:ellipsize="marquee"
                    android:marqueeRepeatLimit="marquee_forever"
                    android:text="@string/bookings_title"
                    android:textSize="16sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/textBalance"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="monospace"
                    android:textSize="16sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <FrameLayout
                android:id="@+id/importBanner"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:visibility="gone">

                <de.spahr.ausgaben.ui.ShimmerView
                    android:id="@+id/importShimmer"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingHorizontal="16dp">

                    <TextView
                        android:id="@+id/importStatus"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:textStyle="bold"
                        android:textColor="@color/import_banner_text"
                        android:maxLines="1"
                        android:ellipsize="end"
                        android:text="@string/import_running_banner" />

                    <TextView
                        android:id="@+id/importPercent"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textStyle="bold"
                        android:fontFamily="monospace"
                        android:textColor="@color/import_banner_text" />
                </LinearLayout>
            </FrameLayout>

            <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
                android:id="@+id/swipeRefresh"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <!--
                    Das Wischen nach unten verträgt nur ein Kind, deshalb der Rahmen: Liste und
                    Leer-Hinweis liegen übereinander. Wichtig ist, dass der Hinweis INNERHALB des
                    Wischbereichs sitzt – sonst ließe sich auf dem leeren Bildschirm nicht mehr
                    nachladen, also genau dort nicht, wo es am nötigsten ist.
                -->
                <FrameLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/recyclerBookings"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipToPadding="false"
                        android:paddingHorizontal="16dp"
                        android:paddingBottom="88dp" />

                    <LinearLayout
                        android:id="@+id/emptyBox"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:orientation="vertical"
                        android:gravity="center_horizontal"
                        android:paddingHorizontal="32dp"
                        android:visibility="gone">

                        <TextView
                            android:id="@+id/emptyText"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="center"
                            android:textColor="@color/grey_text"
                            android:textSize="15sp" />

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/emptyAction"
                            style="@style/Widget.MaterialComponents.Button.TextButton"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:text="@string/empty_reset_filter"
                            android:visibility="gone" />
                    </LinearLayout>
                </FrameLayout>
            </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
        </LinearLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabScrollTop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_margin="16dp"
            android:visibility="gone"
            android:contentDescription="@string/scroll_top"
            app:fabSize="mini"
            app:srcCompat="@drawable/ic_arrow_up"
            app:tint="@color/white"
            app:backgroundTint="@color/green_primary" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="88dp"
            android:contentDescription="@string/number_entry"
            app:fabSize="mini"
            app:srcCompat="@drawable/ic_dialpad"
            app:tint="@color/white"
            app:backgroundTint="@color/green_primary" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fabNew"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="16dp"
            android:text="@string/new_booking"
            android:textColor="@color/white"
            app:icon="@drawable/ic_add"
            app:iconTint="@color/white"
            app:backgroundTint="@color/green_primary" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <LinearLayout
        android:id="@+id/drawerPanel"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:orientation="vertical"
        android:background="?attr/colorSurface"
        android:fitsSystemWindows="true">

        <include layout="@layout/nav_header" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/accountList"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/list_divider" />

        <TextView
            android:id="@+id/addAccount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="52dp"
            android:gravity="center_vertical"
            android:paddingHorizontal="20dp"
            android:paddingVertical="12dp"
            android:drawablePadding="12dp"
            android:text="@string/add_account"
            android:textSize="16sp"
            android:textColor="@color/button_accent"
            app:drawableStartCompat="@drawable/ic_add"
            app:drawableTint="@color/button_accent"
            android:background="?attr/selectableItemBackground" />
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
